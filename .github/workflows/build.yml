name: Test Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test_builds:
    name: ${{ matrix.os }} (${{ matrix.compiler }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest,      compiler: Clang }
          - { os: ubuntu-24.04-arm,   compiler: Clang }
          - { os: windows-latest,     compiler: MSVC }
          - { os: macos-15,           compiler: Clang }

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt-get install -y cmake build-essential ninja-build libjack-jackd2-dev libpipewire-0.3-dev libpulse-dev libsndio-dev libx11-dev libfribidi-dev libthai-dev libgl-dev libgles-dev libdrm-dev libgbm-dev libwayland-dev libxkbcommon-dev libegl-dev libwayland-egl-backend-dev

      - name: Install GCC (Linux)
        if: matrix.compiler == 'GCC' && runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc g++
          echo "CC=gcc" >> $GITHUB_ENV
          echo "CXX=g++" >> $GITHUB_ENV

      - name: Install Clang 22 (Linux)
        if: matrix.compiler == 'Clang' && runner.os == 'Linux'
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 22 all
          echo "CC=clang-22" >> $GITHUB_ENV
          echo "CXX=clang++-22" >> $GITHUB_ENV

      - name: Setup Clang (macOS)
        if: matrix.compiler == 'Clang' && runner.os == 'macOS'
        run: |
          brew install llvm
          LLVM_PREFIX=$(brew --prefix llvm)
          echo "CC=${LLVM_PREFIX}/bin/clang" >> $GITHUB_ENV
          echo "CXX=${LLVM_PREFIX}/bin/clang++" >> $GITHUB_ENV

      - name: Setup Clang (Windows)
        if: matrix.compiler == 'Clang' && runner.os == 'Windows'
        run: |
          choco install llvm -y
          echo "CC=clang" >> $env:GITHUB_ENV
          echo "CXX=clang++" >> $env:GITHUB_ENV
          
      - name: Setup MSVC (Windows)
        if: matrix.compiler == 'MSVC' && runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: cmake gen ${{ runner.os }}
        run: cmake -B build -GNinja

      - name: Build executable ${{ runner.os }}
        run: cmake --build build